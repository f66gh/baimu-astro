---
import Layout from '../../layouts/Layout.astro';
import PageHeader from '../../components/PageHeader.astro';
import { getLocalizedEntries } from '../../lib/content';
import { localizePath, t } from '../../lib/i18n';
import { getMusicTagHue } from '../../lib/music';
import { aboutParagraphs, aboutTags, siteDescriptions, siteEyebrow } from '../../lib/site';

const lang = 'en';
const [anime, notes, music, academic] = await Promise.all([
	getLocalizedEntries('anime', lang),
	getLocalizedEntries('notes', lang),
	getLocalizedEntries('music', lang),
	getLocalizedEntries('academic', lang),
]);

const latest = [...academic, ...notes, ...anime, ...music]
	.sort((a, b) => b.entry.data.date.valueOf() - a.entry.data.date.valueOf())
	.slice(0, 3)
	.map((item) => {
		const { entry, baseId } = item;
		const collection = entry.collection as 'academic' | 'notes' | 'anime' | 'music';
		const isExternal = collection === 'music';

		return {
			entry,
			collection,
			href: isExternal ? entry.data.url : localizePath(`/${collection}/${baseId}/`, lang),
			isExternal,
			label: t(lang, collection),
		};
	});

const stats = [
	{
		label: t(lang, 'academic'),
		value: academic.length,
		href: localizePath('/academic/', lang),
		tone: 'coral',
		description: 'Paper reading, group tasks, and ideas that slowly become research memory.',
	},
	{
		label: t(lang, 'notes'),
		value: notes.length,
		href: localizePath('/notes/', lang),
		tone: 'teal',
		description: 'Learning notes, tool records, and explorations outside the main research thread.',
	},
	{
		label: t(lang, 'anime'),
		value: anime.length,
		href: localizePath('/anime/', lang),
		tone: 'violet',
		description: 'A wall for the shows that keep echoing for a while after the credits are over.',
	},
	{
		label: t(lang, 'music'),
		value: music.length,
		href: localizePath('/music/', lang),
		tone: 'gold',
		description: 'Every favorite piece makes me imagine that one day I might play it well myself.',
	},
];
---

<Layout title="Baimu's Homepage" description={siteDescriptions[lang]}>
	<PageHeader eyebrow={siteEyebrow} title="Baimu's Homepage" lede={siteDescriptions[lang]} />

	<section class="stat-grid" aria-label="Content stats">
		{
			stats.map((item) => (
				<a class:list={['stat-card', `tone-${item.tone}`]} href={item.href}>
					<span>{item.label}</span>
					<strong>{item.value}</strong>
					<small>{item.description}</small>
				</a>
			))
		}
	</section>

	<section class="panel about-panel">
		<div class="about-head">
			<div>
				<h2>{t(lang, 'about')}</h2>
				<p class="identity-line">Graduate student / Backend EDA / STA / desk-to-practice-room commuter</p>
			</div>
			<div class="about-tags">
				{
					aboutTags.map((tag) => (
						<span style={`--tag-hue: ${getMusicTagHue(tag.zh)}`}>{tag.en}</span>
					))
				}
			</div>
		</div>
		<div class="about-copy">
			{aboutParagraphs[lang].map((paragraph, index) => <p class:list={[index === aboutParagraphs[lang].length - 1 && 'quote']}>{paragraph}</p>)}
		</div>
	</section>

	<section class="dashboard-grid">
		<article class="panel publication-panel">
			<h2>{t(lang, 'publications')}</h2>
			<p>{t(lang, 'noPublications')}</p>
		</article>

		<article class="panel timeline-panel">
			<h2>{t(lang, 'latest')}</h2>
			<ul>
				{
					latest.map((item) => (
						<li>
							<a href={item.href} target={item.isExternal ? '_blank' : undefined} rel={item.isExternal ? 'noreferrer' : undefined}>
								<div class="timeline-copy">
									<span class:list={['timeline-badge', `tone-${item.collection === 'academic' ? 'coral' : item.collection === 'notes' ? 'teal' : item.collection === 'anime' ? 'violet' : 'gold'}`]}>
										{item.label}
									</span>
									<strong>{item.entry.data.title}</strong>
								</div>
								<span>{item.entry.data.date.toLocaleDateString('en-US')}</span>
							</a>
						</li>
					))
				}
			</ul>
		</article>
	</section>
</Layout>

<style>
	.panel,
	.stat-card {
		border: 1px solid var(--line);
		border-radius: 8px;
		background: var(--panel);
		box-shadow: 0 2px 10px var(--shadow);
	}

	.stat-grid {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 16px;
		margin: 16px 0;
	}

	.stat-card {
		position: relative;
		display: grid;
		align-content: start;
		gap: 8px;
		min-height: 168px;
		padding: 18px 18px 16px;
		overflow: hidden;
		text-decoration: none;
	}

	.stat-card::before {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 4px;
		background: var(--teal);
		content: "";
	}

	.stat-card span,
	.stat-card strong,
	.stat-card small {
		display: block;
	}

	.stat-card span {
		color: var(--muted);
		font-size: 0.94rem;
		font-weight: 750;
	}

	.stat-card strong {
		font-size: 2rem;
		line-height: 1;
	}

	.stat-card small {
		color: var(--muted);
		font-size: 0.84rem;
		line-height: 1.75;
	}

	.tone-coral::before {
		background: var(--coral);
	}

	.tone-violet::before {
		background: var(--violet);
	}

	.tone-gold::before {
		background: var(--gold);
	}

	.about-panel {
		padding: 20px 22px;
	}

	.about-head {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 16px;
		margin-bottom: 16px;
	}

	.about-head h2,
	.panel h2 {
		margin: 0 0 8px;
		font-size: 1rem;
	}

	.identity-line {
		margin: 0;
		color: var(--muted);
		font-size: 0.92rem;
		line-height: 1.7;
	}

	.about-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 8px;
		justify-content: flex-end;
	}

	.about-tags span {
		padding: 5px 9px;
		border: 1px solid hsl(var(--tag-hue) 76% 70% / 0.6);
		border-radius: 8px;
		background: hsl(var(--tag-hue) 78% 58% / 0.14);
		color: hsl(var(--tag-hue) 56% 48%);
		font-size: 0.82rem;
		font-weight: 800;
	}

	html[data-theme='dark'] .about-tags span {
		color: hsl(var(--tag-hue) 86% 82%);
	}

	.about-copy {
		display: grid;
		gap: 12px;
	}

	.about-copy p,
	.publication-panel p {
		margin: 0;
		color: var(--muted);
		line-height: 1.82;
	}

	.about-copy .quote {
		color: var(--ink);
		font-weight: 800;
	}

	.dashboard-grid {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(300px, 0.9fr);
		gap: 16px;
		margin-top: 16px;
	}

	.panel {
		padding: 18px;
	}

	.timeline-panel ul {
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.timeline-panel li + li {
		border-top: 1px solid var(--line);
	}

	.timeline-panel a {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 14px;
		padding: 12px 0;
		text-decoration: none;
	}

	.timeline-copy {
		display: grid;
		gap: 8px;
		min-width: 0;
	}

	.timeline-copy strong {
		font-size: 0.95rem;
		line-height: 1.55;
	}

	.timeline-panel > ul span:last-child {
		flex: 0 0 auto;
		color: var(--muted);
		font-size: 0.84rem;
	}

	.timeline-badge {
		width: fit-content;
		padding: 3px 8px;
		border: 1px solid var(--line);
		border-radius: 8px;
		background: var(--tag-bg);
		font-size: 0.74rem;
		font-weight: 800;
	}

	.timeline-badge.tone-coral {
		color: var(--coral);
	}

	.timeline-badge.tone-teal {
		color: var(--teal);
	}

	.timeline-badge.tone-violet {
		color: var(--violet);
	}

	.timeline-badge.tone-gold {
		color: var(--gold);
	}

	@media (max-width: 960px) {
		.stat-grid {
			grid-template-columns: repeat(2, 1fr);
		}

		.dashboard-grid {
			grid-template-columns: 1fr;
		}

		.about-head {
			flex-direction: column;
		}

		.about-tags {
			justify-content: flex-start;
		}
	}

	@media (max-width: 520px) {
		.panel,
		.about-panel {
			padding: 16px;
		}

		.stat-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
			gap: 10px;
		}

		.timeline-panel a {
			align-items: flex-start;
			flex-direction: column;
		}
	}

	@media (max-width: 360px) {
		.stat-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
