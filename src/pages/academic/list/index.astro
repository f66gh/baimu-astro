---
import CollectionPage from '../../../components/content/CollectionPage.astro';
import PageHeader from '../../../components/PageHeader.astro';
import Layout from '../../../layouts/Layout.astro';
import { getLocalizedEntries, listPageSize, paginateItems } from '../../../lib/content';
import { t } from '../../../lib/i18n';

const lang = 'zh';
const page = paginateItems(await getLocalizedEntries('academic', lang), 1, listPageSize);
---

<Layout title={t(lang, 'titleWithSite', { title: `${t(lang, 'academic')}${t(lang, 'listSuffix')}`, site: t(lang, 'siteShortName') })}>
	<PageHeader eyebrow={t(lang, 'academicEyebrow')} title={t(lang, 'academic')} lede={t(lang, 'academicListLede')} />
	<CollectionPage
		entries={page.items}
		collection="academic"
		lang={lang}
		view="list"
		basePath="/academic"
		currentPage={page.currentPage}
		totalPages={page.totalPages}
	/>
</Layout>
