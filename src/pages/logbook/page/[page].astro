---
import CollectionPage from '../../../components/content/CollectionPage.astro';
import PageHeader from '../../../components/PageHeader.astro';
import Layout from '../../../layouts/Layout.astro';
import { cardPageSize, getLocalizedEntries, numberedPagePaths, paginateItems } from '../../../lib/content';
import { t } from '../../../lib/i18n';

const lang = 'zh';

export async function getStaticPaths() {
	const entries = await getLocalizedEntries('logbook', 'zh');
	return numberedPagePaths(entries, cardPageSize);
}

const { currentPage } = Astro.props;
const page = paginateItems(await getLocalizedEntries('logbook', 'zh'), currentPage, cardPageSize);
---

<Layout title={t(lang, 'titleWithPageLabel', { title: t(lang, 'logbook'), pageLabel: t(lang, 'page', { page: currentPage }) })}>
	<PageHeader eyebrow={t(lang, 'logbookEyebrow')} title={t(lang, 'logbook')} lede={t(lang, 'homeLogbookDescription')} />
	<CollectionPage
		entries={page.items}
		collection="logbook"
		lang={lang}
		view="card"
		basePath="/logbook"
		currentPage={page.currentPage}
		totalPages={page.totalPages}
	/>
</Layout>
