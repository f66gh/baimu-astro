---
import MusicCategoryPage from '../../../../components/music/MusicCategoryPage.astro';
import PageHeader from '../../../../components/PageHeader.astro';
import { paginateItems, getLocalizedEntries } from '../../../../lib/content';
import { filterMusicGroup, getMusicGroupLabel, musicPageSize } from '../../../../lib/music';
import { t } from '../../../../lib/i18n';
import Layout from '../../../../layouts/Layout.astro';

const lang = 'zh';
const entries = filterMusicGroup(await getLocalizedEntries('music', lang), 'piano');
const page = paginateItems(entries, 1, musicPageSize);
---

<Layout title={t(lang, 'titleWithSite', { title: getMusicGroupLabel('piano', lang), site: t(lang, 'siteShortName') })}>
	<PageHeader eyebrow={t(lang, 'musicEyebrow')} title={getMusicGroupLabel('piano', lang)} lede={t(lang, 'pianoLede')} />
	<MusicCategoryPage
		group="piano"
		entries={page.items}
		basePath="/music/practice/piano"
		currentPage={page.currentPage}
		totalPages={page.totalPages}
		lang={lang}
	/>
</Layout>
