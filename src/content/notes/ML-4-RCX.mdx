---
title: "ML for RCX"
summary: "读文献"
date: 2026-06-02
tags: ["机器学习", "RCX"]
cover: "cover.jpg"
imageRoot: "/images/notes/ML-4-RCX"
---
## 综述

参考文献：Deep Learning Inspired Capacitance Extraction Techniques（清华 ASPDAC 2025）

### 场求解器

电容是与导体结构相关的一种属性，导体携带电容可以用电荷和电势（电压）求解。我们把电容看成一个矩阵，对角线$C_{ii}$是导体的**总电容（自电容）**，非对角线是$C_{ij}$是导体间的**耦合电容**。将主导体设为1v电势，其余导体设为0v，那么各导体的电荷量就等于电荷矩阵C中与该导体对应的那一列。


这就将问题转化为了模拟3D电场以计算电荷。因此可以使用有限差分法（FDM）、有限元法（FEM）和边界元法（BEM）等数值方法。

场求解器主要流程是：

1. 求解电势与电场，这里$\phi$：电势；$\varepsilon$：介电常数；$∇\phi$：电势变化率。

$$
∇ \cdot (\varepsilon ∇ \phi) = 0,\quad where~ \mathbf{E} = -∇\phi
$$

2. 求电荷，电荷来自电场在导体表面的法向通量，由高斯公式可知，对导体表面积分：
$$
Q = \oiint_S \mathbf {D} \cdot d\mathbf{S} = \oiint_S \varepsilon \mathbf {E} \cdot d\mathbf{S}  =  \oiint_S \varepsilon \mathbf {E} \cdot \mathbf{n} dS
$$

3. 根据电势和电荷反推出电容

$$
Q = CU
$$

详细流程如下：

#### 电容矩阵与一次激励

<NoteImage
	src="场求解器1.jpg"
	size="md"
/>

先假设空间中有三个导体 $A,B,C$ 和一个基底 substrate。我们想计算导体 $A$ 与其它导体、基底之间的耦合电容。场求解器通常会做一次“单位电压激励”：令

$$
U_A = 1,\quad U_B = 0,\quad U_C = 0,\quad U_{sub} = 0
$$

已知基本关系：

$$
Q = CU
$$

其中 $Q$ 是导体表面电荷，$C$ 是电容，$U$ 是电势差。对于导体 $A$，它的电荷可以写成它与其它导体、基底之间电势差贡献的和：

$$
Q_A =
C_{AB}(U_A-U_B)
+ C_{AC}(U_A-U_C)
+ C_{A,sub}(U_A-U_{sub})
$$

如果考虑所有导体的总电荷，可以写成 Maxwell 电容矩阵（Maxwell capacitance matrix，麦克斯韦电容矩阵）的形式：

$$
\begin{bmatrix}
Q_A\\
Q_B\\
Q_C
\end{bmatrix}
=
\begin{bmatrix}
C_{AB}+C_{AC}+C_{A,sub} & -C_{AB} & -C_{AC}\\
-C_{BA} & C_{BA}+C_{BC}+C_{B,sub} & -C_{BC}\\
-C_{CA} & -C_{CB} & C_{CA}+C_{CB}+C_{C,sub}
\end{bmatrix}
\begin{bmatrix}
U_A\\
U_B\\
U_C
\end{bmatrix}
$$

这里对角线项表示某导体的总电荷响应，非对角线项带负号，表示其它导体上被感应出来的耦合电荷贡献。由于本次激励中只有 $U_A=1$，其余导体和基底都是 $0$，所以有：

$$
Q_A = C_{AB}+C_{AC}+C_{A,sub},\quad
Q_B = -C_{BA},\quad
Q_C = -C_{CA}
$$

所以，只要场求解器能在这次激励下算出各个导体表面的电荷，就能得到电容矩阵中与导体 $A$ 对应的一列。依次把 $A,B,C$ 分别置为 $1V$，其它导体置为 $0V$，就可以抽取完整电容矩阵。

#### 从电场到表面电荷

<MediaPair
  src="场求解器2.jpg"
  imageSize="xs"
>
麦克斯韦方程中，电位移矢量 $\mathbf{D}$ 与自由电荷密度 $\rho_f$ 满足：

$$
\nabla \cdot \mathbf{D} = \rho_f
$$

散度可以理解为“某一点向外发射或向内吸收场线的程度”，这里的物理含义是**这一点吸引或散发多少电场**。

如果 $\mathbf{D}=(D_x,D_y,D_z)$，则：

$$
\text{div}~\mathbf{D}
=
\nabla \cdot \mathbf{D}
=
\frac{\partial D_x}{\partial x}
+\frac{\partial D_y}{\partial y}
+\frac{\partial D_z}{\partial z}
$$

</MediaPair>


为了求某个导体表面携带的总电荷 $Q$，可以假设导体被一个闭合曲面 $S$ 包围，这个曲面围起来的体积为 $V$。由高斯公式（高斯散度定理）：

$$
\iiint_V \nabla \cdot \mathbf{F}\,dV
=
\iiint_V \frac{\partial D_x}{\partial x}+\frac{\partial D_y}{\partial y}+\frac{\partial D_z}{\partial z}\,dV
=
\oiint_S \mathbf{F}\cdot d\mathbf{S}
$$

对麦克斯韦方程左右两边积分，可得：

$$
\iiint_V \nabla \cdot \mathbf{D}\,dV
=
\oiint_S \mathbf{D}\cdot d\mathbf{S}
$$

又因为 $\nabla \cdot \mathbf{D}=\rho_f$，所以：

$$
Q
=
\iiint_V \rho_f\,dV
=
\oiint_S \mathbf{D}\cdot d\mathbf{S}
$$

其中 $\varepsilon$ 是介电常数，$\mathbf{E}$ 是电场强度。电场可以看成电势的空间变化率：

$$
\mathbf{E}=-\nabla \phi
$$

因此导体表面电荷也可以写成：

$$
Q
=
\oiint_S \mathbf{D}\cdot d\mathbf{S}
=
\oiint_S \varepsilon \mathbf{E}\cdot d\mathbf{S}
=
\oiint_S \varepsilon \mathbf{E}\cdot \mathbf{n}\,dS
$$

其中 $d\mathbf{S}=\mathbf{n}dS$，$\mathbf{n}$ 是垂直于表面的单位法向量。$\mathbf{E}\cdot\mathbf{n}$ 表示电场在法向方向上的分量，因为只有垂直穿过导体表面的电场线会对表面电荷有贡献。

#### 从导体周围电势梯度计算电场

在导体之外的介质区域中没有自由电荷，所以 $\rho_f=0$。

$$
\nabla \cdot \mathbf{D}=0
$$

电位移矢量$\mathbf{D}$和电场强度$\mathbf{E}$满足，这里的电位移矢量指的是从一个点散发或吸收的电场，电场强度是描述整个空间的电场：

$$
\mathbf{D}=\varepsilon\mathbf{E}
$$

由上面两个公式可得：

$$
\mathbf{D}=\varepsilon\mathbf{E}=\varepsilon(-\nabla\phi)
$$

可得场求解器真正要求解的电势方程：

$$
\nabla\cdot(\varepsilon\nabla\phi)=0
$$

也就是说：已知介电常数 $\varepsilon$ 和导体边界电势，先求空间中的电势 $\phi$，再由 $\mathbf{E}=-\nabla\phi$ 得到电场，最后通过表面积分得到导体电荷 $Q$，进而反推出电容 $C$。

在计算机里，连续空间会被离散成很多小块或网格，常见方法就是 FDM 和 FEM。

<iframe
	class="note-interactive"
	src="/interactives/field-solver-gemini/index.html"
	title="场求解器原理交互演示"
	loading="lazy"
></iframe>

场求解器本质是求电荷和导体周围的变化量，下面是场求解器的介绍：

#### FDM与FEM

FDM与FEM本质是求导体周围的电势变化量。

以二维FDM为例，场求解器会先把连续空间切成一个个规则小方格，只在格点上保存未知电势。对于一个不在导体边界上的内部格点 $\phi_{i,j}$，二维 Laplace 方程可以等价地写成，含义是这块的电势是上下左右四个电势的平均值：

$$
-4\phi_{i,j}+\phi_{i+1,j}+\phi_{i-1,j}+\phi_{i,j+1}+\phi_{i,j-1}=0
$$

如果某个邻居是固定电压边界，比如左导线为 $1V$，右导线或 substrate 为 $0V$。如果总共有 $n$ 个未知格点，$\boldsymbol{\phi}$ 就是长度为 $n$ 的向量，$A$ 是 $n \times n$ 的稀疏矩阵。只是因为二维五点模板只看上下左右四个邻居，所以 $A$ 的每一行最多只有 5 个非零数：中心格点的 $-4$，以及邻居位置上的 $1$。

举一个最小的 $2 \times 2$ 未知格点例子。假设中间有 4 个未知格点

这里：

$$
\boldsymbol{\phi}=
\begin{bmatrix}
x_1\\
x_2\\
x_3\\
x_4
\end{bmatrix}
$$

对 $x_1$ 来说，左边邻居是 $1V$ 边界，上边邻居是 $0V$ 边界，右边是 $x_2$，下边是 $x_3$，所以：
$
-4x_1+x_2+x_3=-1
$

对 $x_2$、$x_3$、$x_4$ 来说，同理。

把四个方程合起来就是一个具体的矩阵方程：

$$
A\boldsymbol{\phi}=\boldsymbol{b}
$$

$$
\begin{bmatrix}
-4 & 1 & 1 & 0\\
1 & -4 & 0 & 1\\
1 & 0 & -4 & 1\\
0 & 1 & 1 & -4
\end{bmatrix}
\begin{bmatrix}
x_1\\
x_2\\
x_3\\
x_4
\end{bmatrix}
=
\begin{bmatrix}
-1\\
0\\
-1\\
0
\end{bmatrix}
$$

对于真实芯片局部窗口，未知格点可能有几十万甚至更多，所以 $A$ 会非常大，而且是算上下前后左右六个格点的影响值；但它是稀疏矩阵，因为每个格点只直接连接少数邻居。

#### BEM

BEM本质是直接求导体表面电荷。

BEM代表是FastCap。已知边界电势$v$，直接求表面电荷$q$

$$
Pq = v
$$

BEM并不需要求解空间中的电势矩阵。由库仑定律，在均匀介质中，设$q$是点电荷带电量，一个点电荷放在$\mathbf{r}_0$点，对$\mathbf{r}$点的电势为：

$$
\phi(\mathbf{r}) = \frac{1}{4 π \varepsilon} \frac{q}{|\mathbf{r} - \mathbf{r}_0|}
$$

把带有电荷的发射板切分为一个个点，每一个点都带电荷，对其积分。物理含义是**在这个平面上，把所有观察点r的微小电荷贡献加起来，计算这些电荷对一个接收板中心点的电势**。
其中，$\mathbf{r}$是接收板中心点坐标，$\mathbf{r}'$是发射板观察点，$\sigma(\mathbf{r}')$是发射板积分区域电荷密度：

$$
\phi(\mathbf{r}) = \frac{1}{4 π \varepsilon} \int_{S} \frac{\sigma(\mathbf{r}')}{|\mathbf{r} - \mathbf{r}'|}dS'
$$

BEM中，我们假设电荷是均匀涂抹在积分区域中，所以密度$\sigma$是个常数，可以拿出来。我们关心的是导体表面总电荷，在积分区域内总电荷满足面积成密度：$q=A\sigma$。
于是，上述公式可以转化为：

$$
\begin{aligned}
\phi(\mathbf{r}) 
&= \frac{\sigma(\mathbf{r}')}{4 π \varepsilon} \int_{S} \frac{1}{|\mathbf{r} - \mathbf{r}'|}dS'\\
&= \frac{q}{4 π \varepsilon A} \int_{S} \frac{1}{|\mathbf{r} - \mathbf{r}'|}dS'\\
&= P q
\end{aligned}
$$

其中，发射板设为$j$，接收板设为$i$，$r'$是在发射板上面的观察点，我们把$P_{ij}$作为系数提出来：

$$
P_{ij} = \frac{1}{4 π \varepsilon A_j} \int_{S_j} \frac{1}{|\mathbf{r}_i - \mathbf{r}'|}dS'
$$

然后通过$Pq = v$矩阵，转化为$q=P^{-1}v$，求解发射板上每一个单位面积的电荷量。这里$P$叫做电容系数矩阵，是一个稠密矩阵。

$$
\phi_i = P_{i1}q_1 + P_{i2}q_2 + \cdots + P_{iN}q_N
$$

<MediaPair src="场求解器3.jpg" imageSize="xs">
我们以一个二维简化的玩具模型为例，并且只计算导体A底部这一个单位面的感应电荷：

我们假设A是1v，B和substrate都是0v，以草稿纸一个格子长度为单位面，那么从A底部单位面发射的电场线如图所示。

我们定义导体B的上下左右单位面和substrate两个单位面的符号是$b_t,b_r,b_b,b_l,s_1,s_2$，有未知电荷量为：

$$
q = \begin{bmatrix} q_a & q_{b_t} & q_{b_r} & q_{b_b} & q_{b_l} & q_{s_1} & q_{s_2} \end{bmatrix}^T
$$

电压向量为：

$$
v = \begin{bmatrix} 1 & 0 & 0 & 0 & 0 & 0 & 0 \end{bmatrix}^T
$$


</MediaPair>

把$Pq=v$展开为：

$$
\begin{bmatrix} 
P_{a,a} & P_{a,b_t} & P_{a,b_r} & P_{a,b_b} & P_{a,b_l} & P_{a,s_1} & P_{a,s_2} \\
P_{b_t,a} & P_{b_t,b_t} & P_{b_t,b_r} & P_{b_t,b_b} & P_{b_t,b_l} & P_{b_t,s_1} & P_{b_t,s_2} \\
P_{b_r,a} & P_{b_r,b_t} & P_{b_r,b_r} & P_{b_r,b_b} & P_{b_r,b_l} & P_{b_r,s_1} & P_{b_r,s_2} \\
P_{b_b,a} & P_{b_b,b_t} & P_{b_b,b_r} & P_{b_b,b_b} & P_{b_b,b_l} & P_{b_b,s_1} & P_{b_b,s_2} \\
P_{b_l,a} & P_{b_l,b_t} & P_{b_l,b_r} & P_{b_l,b_b} & P_{b_l,b_l} & P_{b_l,s_1} & P_{b_l,s_2} \\
P_{s_1,a} & P_{s_1,b_t} & P_{s_1,b_r} & P_{s_1,b_b} & P_{s_1,b_l} & P_{s_1,s_1} & P_{s_1,s_2} \\
P_{s_2,a} & P_{s_2,b_t} & P_{s_2,b_r} & P_{s_2,b_b} & P_{s_2,b_l} & P_{s_2,s_1} & P_{s_2,s_2} 
\end{bmatrix} \begin{bmatrix} q_a \\ q_{b_t} \\ q_{b_r} \\ q_{b_b} \\ q_{b_l} \\ q_{s_1} \\ q_{s_2} 
\end{bmatrix} = \begin{bmatrix} 1 \\ 0 \\ 0 \\ 0 \\ 0 \\ 0 \\ 0 
\end{bmatrix}
$$

转化为$q=P^{-1}v$，求解导体的表面附着总电荷，然后带回$Q=CU$，继续求麦克斯韦电容矩阵。

在真实情况中，空间是三维的，而且也要计算B和sub对于A的感应电荷，这个电荷系数矩阵同样非常庞大，而且是稠密的。

### 浮动随机游走

浮动随机游走（floating random walk）方法是一种特殊的场求解器技术，它不涉及求解线性方程组这一极其消耗内存的任务，因此具备处理更大结构的能力。hop是跳数，threshold是距离导体/sub多近算终止。

求解电势：某个点的电势，可以看成从这个点出发并随机游走撞到各个边界电势的平均结果。

浮动：每一个hop都会以这个点为中心构建一个trastion cube，cube尽量大但不能碰到导体边界，然后在这个cube边界选一个点，作为下一个hop的中心点。

采样：transition cube不能乱采样。对于每次跳跃，立方体边界上的采样点必须服从表面上的随机分布，其概率密度函数被称为**表面格林函数 Surface Green**。这个概率密度函数很难计算，所以为了加速也是用各种方法近似的，但是对于复杂芯片的计算也有误差。因此，迫切需要一种快速而准确的方法，以快速获取具有复杂电介质配置的立方体的表面格林函数。深度学习技术可以用于预测这种函数，转化为ML回归问题。

<iframe
	class="note-interactive"
	src="/interactives/floating-random-walk/index.html"
	title="浮动随机游走 FRW 交互演示"
	loading="lazy"
></iframe>

## 随机森林

参考文献：MLParest: Machine Learning based Parasitic Estimation for Custom Circuit Design(Intel DAC 2020)

问题：在模拟电路流程中（和自动化数字不太一样），layout前预测的寄生参数比layout后提取的真实寄生参数差的很远。输入互联net的各种属性

ML输入：net连接数量、跨越层级数量、连接到MOS drain gate source的数量...

ML输出：等效电容Ceff和等效电阻Reff

方法：训练随机森林，给每个net训练出一个Ceff和Reff。把互联net看成一个星型拓扑结构，整个net有一个电容和多个电阻，连接到各个pin的电阻是Reff除以pin的个数。

<MediaPair
  src="MLParset.jpg"
  imageSize="md"
>
训练集的电容Ceff是post Layout提取出来的真实寄生电容，电阻没用提取出来的电阻，因为提取出的电阻也是一段一段的，而且一个net的所有电阻不能简单累加成一个Reff。所以论文用一个等效公式，用真实的寄生电容算出真实的寄生电阻。

这里我们定义了有效时间常数 ($\tau_{eff}$) 的概念 。对于一个拥有 $N$ 个极点（poles，$p_1, p_2, ..., p_N$）的系统，其有效时间常数定义如下 ：
$$
\tau_{eff}=\sqrt{\frac{1}{{p_{1}}^{2}}+\cdot\cdot\cdot+\frac{1}{{p_{i}}^{2}}+\cdot\cdot\cdot+\frac{1}{{{p_{N}}^{2}}}}
$$

</MediaPair>

可得有效电阻：
$$
R_{eff} = \frac{\tau_{eff}}{C_{eff}}
$$
每个支路电阻就是：
$$
R_{branch} = \frac{R_{eff}}{M}
$$

## 卷积神经网络

参考文献：CNN-Cap: Effective Convolutional Neural Network-basedCapacitance Models for Interconnect Capacitance Extraction （清华 TODAES 2023 CCFB）

这篇论文用卷积神经网络计算寄生电容。我比较喜欢这篇论文的思想，有点花小钱办大事的感觉。论文只是预测了三层的芯片区域，用场求解器计算master和target之间的耦合电容然后计算，没有扩展到更大的区域。论文训练了两个模型，分别用于预测total电容和耦合电容。

<NoteImage
	src="CNN-Cap_Fig6.png"
/>

一开始，论文的2021年版本是只是对2.5D芯片做的寄生电容预测，编码大概是：

论文把层看成channel，由于每一层的导线属性都差不多，所以文本用了一种取巧的方法，把输入的每一层变为一维的特征向量。每一层输入特征向量有两个，分为从前面看和从侧面看（视角正交）。

特征向量用正交的视角有一个好处：这样顾及到导线的四个面对于同层和不同层之间的绕射电容。如Fig2所示，比如我要计算上面的导线对下面的导线的耦合电容，是$C_{1f1}+C_{1f2}+C_{2f1}+C_{2f2}+C_{1o}$，注意$C_{1o}$和$C_{2o}$是一个东西。

<NoteImage
	src="CNN-Cap_Fig3.jpg"
/>

论文对于特征值的编码也很取巧，如图所示，是在一个单元格内按照面积占比编码，master导线额外加1，target导线变为负值。论文的模型是这样输入输出的：我一次只预测一个导线的total电容或者一对导线的电容，然后只会输出一个值，这样有效避免了因为数据集中的导线数量不同导致的输出数据个数不同。

<NoteImage
	src="CNN-Cap_Fig7.jpg"
/>

这样有一个最严重的问题：无法考虑到同一层中有两个导线不完全重叠的情况，这种情况下侧面视图无法得到有效信息。为此，论文自己也在23年提出了3D的改进方案：

仍然把一层看成一个Channel，但是每一个channel变成了一个有x-y轴的二维图像，这个图像就是从上向下对于这一层的俯视图，如图所示，编码同2.5D那个。

<NoteImage
	src="CNN-Cap_Fig9.jpg"
/>

论文对于一根导线能与其他导线耦合的最大范围边界定为了5um，这个范围叫window。其中windows内有0.5um的padding，以及4um×4um大小的core-region。当master导线完全在core-region内时，忽略window外的导线其的耦合电容，只计算window内的；当master导线很长的时候，考虑到性能问题，没有加大window而是对master导线进行截断，只考虑在core-region内的master导线，只对window内的导线有耦合电容（包含在padding区域内的dummy master）。

<NoteImage
	src="CNN-Cap_Fig13.jpg"
/>

## 注意力机制

### ParaGraph

参考文献：ParaGraph:Layout Parasitics and Device Parameter Prediction using Graph Neural Networks(Nvidia DAC 2020)

<MediaPair
	src="ParaGraph_Fig3.jpg"
	imageSize="md"
>
问题：在模拟电路中，在pre layout时，估计电路模块寄生参数总是凭经验。本文提出了基于各种图神经网络并提出了新的适配模块级的寄生电容和其他相关参数图神经网络

ML输入：晶体管的ploy长度、鳍片个数、电阻长度、...

ML输出：晶体管LDE参数 源极漏极面积和参数 寄生电容（没有电阻）

方法：
在图神经网络的基础上，结合了RGCN(不同边区别对待)，GAT(自注意力抓重点)，GraphSage(用自己的旧特征和新特征拼接)。论文把电路图转化为异构图，pin和导线都设置为节点，节点间用有向边连。由于不同器件的电容差距过大，所以论文根据电容范围把器件分为四个类别，训练了四组模型。
</MediaPair>

邻接边的种类有四个：导线→源极漏极，导线→栅极，源极漏极→导线，源极漏极→导线。边不同，则对应的参数权重矩阵不同。

![](ParaGraph.jpg)

1. 根据邻接边的种类，把传过来的其余节点嵌入分类

2. 用自注意力机制，把各种嵌入加权处理为一个嵌入

分类关系收集：

$$
h_{N(i)}^{(l+1)} = \sum_{r \in R} \sum_{j \in N^{r}(i)} \frac{1}{c_{i,r}} W_{r}^{(l)} h_{j}^{(l)}
$$

3. 把多个新嵌入变成一个新嵌入，然后拼接到自己的旧嵌入上

算出原始权重：

$$
e_{ij}^{l} = \vec{a}^{T} \text{concat}(W^{(l)}h_{i}^{l}, W^{(l)}h_{j}^{l})
$$

权重归一化：

$$
\alpha_{ij}^{l} = \text{softmax}_{i}(\text{LeakyReLU}(e_{ij}^{l}))
$$

算出，这里的$N(i)$和$W$是各种不同边集合/权重矩阵的简写，有不同的W对应不同的N，最后都要加和。

$$
h_{i}^{(l+1)} = \sigma(\sum_{j \in N(i)} \alpha_{i,j} W^{(l)} h_{j}^{(l)})
$$


4. 把新的长嵌入再压回到原来的长度

$$
h_i^{(l+1)} \leftarrow \sigma \left( W^{(l)} \cdot \text{concat}(h_i^{(l)}, h_i + b^{(l)}) \right)
$$

### CircuitGPS

参考文献：Few-shot Learning on AMS Circuits and Its Application to Parasitic Capacitance Prediction(清华 DAC 2025)

论文是在GraphGPS基础上改进的，针对的是模拟电路的后端流程。文中提到ParaGraph是把一个集总的电网做总电容的预测，而没有考虑到耦合电容的计算。
论文是先做了一步预测，看看两个节点之间有没有耦合连接，如果有的话，再预测这个连接的电容。

论文是这样建模的：图中有三类节点，分别是网络节点、引脚节点和器件节点。其中，器件节点和自己的引脚有边，网络和引脚有边。
论文是预测的耦合连接，和图中的拓扑边不是一个东西。

<NoteImage size="sm" src="CircuitGPS_Fig1.jpg"/>

论文对于训练集是这样划分的：图中一对节点在各自的一跳范围内的节点集合有交叉，则把这两个节点的一跳子图的并集当成一个训练和测试用子图。
论文是拿着post-layout后的结果当做真实值，两个节点有耦合电容标签为1，两个节点没有耦合电容标签为0。

网络结构如下图所示，输入网表后，兵分两路。下面的是一些电路相关的特征，上面是寻找并构建子图。
在预训练中，输入的X/d/E分别是节点属性、子图内各节点到要预测的节点的距离以及边属性。预训练的输出是二分类，是否有耦合连接。
在微调中，拿着预训练输出的隐藏嵌入，做了回归任务，预测耦合电容。

<NoteImage size="lg" src="CircuitGPS_Fig2.jpg"/>

## 图神经网络

### Parasitic-Aware

参考文献：Parasitic-Aware Analog Circuit Sizing with Graph Neural Networks and Bayesian Optimization(Nvidia DATE 2021)

<MediaPair
	src="Parastic-Aware_Fig2.jpg"
	imageSize="sm"
>
上一篇论文的续作。上一篇论文主要讲的是集总电容（对地、基底等电容），这篇论文在图神经网络上做了改进，用一对边节点的嵌入计算耦合电容（导线之间电容），用相邻边节点和引脚节点的嵌入计算电阻。加上了导线间的耦合电容后，准确度有明显的改进。

在论文中，把一段导线近似为一个net 模型，里边可能有很多分支，两端可能连很多单元，甚至还有可能跨层，对于大型芯片来说这种预测不够准。

同时，论文没有明确提出用于计算的耦合电容的两个边节点是怎么来的，只是说所有边两两算耦合电容没必要。
</MediaPair>

### GNN-Cap

参考文献：GNN-Cap: Chip-Scale Interconnect Capacitance Extraction Using Graph Neural Network(复旦 TCAD 2024)

方法：论文中把一段net分为一个个cuboid（和tile类似，以下称为tile）。论文把tile当成一个节点，三个特征为长宽高。论文的边有七个特征，分别为两个tile中心的距离，两个tile的X/Y/Z方向坐标差。

论文在水平方向定义了阈值$d_e$(和halo类似，以下称为halo)，在halo范围内，则认为两个节点有边。论文在垂直方向，给每个tile上下加上比层间绝缘层高一点的的halo，目的是让两个tile之间能被识别为边。

<MediaPair
	src="GNN-Cap_Fig4.jpg"
>
	<NoteImage
		src="GNN-Cap_Fig2.jpg"
		size="md"
	/>
</MediaPair>

论文中把芯片建模成这样：

<NoteImage
	src="GNN-Cap_Fig1.jpg"
	size="sm"
/>

本文中主要计算三种电容：自带电容、耦合电容、隐藏边导致的电容。其中，自带电容用节点自己的嵌入算，耦合电容用两个节点和之间的边算。论文中提到，在同层中当两个tile相距过远的时候，中间没有tile隔着的时候，本文的建模方法是不会给这两个tile连上边的，即使这两个tile存在耦合电容；同理，当上下层的两个tile中间隔一层，但是这一层没有tile遮挡的时候，这两个tile也是有耦合电容的，但是本文的建模方法则认为这两个tile之间没有边。因此，本文加了一些这样的虚拟边，但是也不参与GCN的计算。

<MediaPair
	src="GNN-Cap_Fig5.jpg"
>
公式中，$h$是节点嵌入，两个$W$是两个权重矩阵，一个作用与聚合的边，一个作用于节点的旧嵌入，$c$是每两个节点之间的消息缩放比例。

$$
h_{v_i}^{l} = \phi \left( W^{l-1} \sum_{v_j \in N(v_i)} \frac{1}{c(v_i, v_j)} h_{v_j}^{l-1} + W_0^{l-1} h_{v_i}^{l-1} \right)
$$

这是边嵌入的计算公式：

$$
h_{e_{ij}}^{l} = \text{MLP} \left( \text{CONCAT} \left( h_{v_i}^{l}, h_{v_j}^{l}, \text{MLP}_{trans}(h_{e_{ij}}^{l-1}) \right) \right)
$$
</MediaPair>

由于算的都是按照一个个tile或者tile之间的电容，寄生参数提取下一步的STA要每个网络（一段由一个或几个tile拼接的抽象长导线）的电容，所以把一段net内的tile电容都加和。

论文的标签是场求解器得到的值，论文中只用了一个开源的28nm工艺库和八个不同的芯片设计用例。速度比场求解器快。论文用了平均相对误差MARE，也就是每个电容都算一个相对误差，再平均。论文是这样比较的：自己的算法和场求解器算一个MARE，用starRC自己的快速估值和starRC自己的场求解器算一个MARE，结果更好一些。

$$
\text{MARE} = \frac{1}{N} \sum_{i=1}^{N} \left|    1 - \frac{f(x_i)}{y_i} \right|
$$

论文是拿了20um×20um的芯片窗口做的。

### DL Pre-Layout

参考文献：Deep-Learning-Based Pre-Layout Parasitic Capacitance  Prediction on SRAM Designs（清华/港中文 GLSVLSI 2024 CCFC）

在布局布线之前，做好寄生电容预测。SRAM内部网络电容分布不均衡，有大量小电容网络，训练时候占据绝大多数样本。但是大电容net就容易预测不好，这些往往是对于寄生参数有很大影响的。

论文把网表转化为了异构图，图中有三类节点：网络节点、器件节点、子电路节点。子电路节点是由若干器件组成的，加上这个节点特征可以反应网表的层次结构。论文构建的图是把这三个节点混在一起，用无向边连接的，没有具体展开是怎么连的。

论文把模型分为两个阶段：第一阶段是GNN 分类，第二个是MLP 回归。第一阶段只判断这个net大概属于哪个电容数量级，第二个阶段才是回归。

论文用了Focal Loss，SRAM数据中小电容net太多，用普通cross entropy模型会被简单样本主导，于是用了Focal Loss，直观作用是少数类样本提高关注度。

### NAS-Cap 

参考文献：Training Better CNN Models for 3-D Capacitance  Extraction with Neural Architecture Search(清华 DATE CCFB 2024)

论文在复旦的那个CNN-Cap上改进的，不人为指定卷积网络结构，而是给出几个结构，让模型自己挑。论文说CNN-Cap预测总电容还不错，但是预测耦合电容误差较大，这篇论文主要改进这个点。

下面公式中，$\hat{o}$是多个操作的候选结果，$\theta$是操作被选中的概率权重，$O$是候选操作集合。
$$
\hat{o}^{(i,j)}(x)=\sum_{o\in O}\theta_o^{(i,j)}o(x), \quad \text{s.t.}~\sum_{o\in O}\theta_o^{(i,j)} = 1
$$

### CircuitGCL

参考文献：Transferable Parasitic Estimation via Graph Contrastive Learning and Label Rebalancing in AMS Circuits（南京理工/清华/北邮 ICCAD 2025 CCFB）

AMS(Analog-Mixed Signal)是模拟混合信号，即电路中又有模拟信号，又有数字信号。论文解决的是AMS电路中训练数据少，不同电路差异大，寄生电容标签分布严重不均衡导体GNN很难跨电路泛化问题。

论文主要预测pre-layout时候的寄生电容，论文把节点分成了net/device/pin，边主要是device-pin和net-pin。

论文为了简化，把异构图当成了同构图，把不同节点当成特征属性输入。论文增强了节点特征。

<NoteImage src="CircuitGCL_Fig2.jpg" size="lg"/>

由于标签少，标签分布不均匀，所以论文用了自监督学习，用了两个encoder。
训练时，target encoder用于教学，online encoder用于学习。测试和验证时候只用online encoder用于学习。
测试和验证时候只用online encoder。

## 其余方法

### PCT-Cap

参考文献：PCT-Cap: Point Cloud Transformer for Accurate 3D Capacitance Extraction（iEDA团队/清华/东南/中科院 ISEDA 2024 无评级）

注：ISEDA 是国内发起并主导的新兴 EDA 国际会议，2023年举办首届。由于创办时间较短，它尚未被纳入CCF推荐的A/B/C类会议目录中。

把真实 3D 几何和介质边界采样成点云，让 Transformer 学 3D 空间关系。对我的启发是可以在底层公式中寻找一些特征作为神经网络输入，这样比单纯的黑盒模型更有说服力。

<NoteImage src="PCT-Cap_Fig4.jpg" size="md"/>

论文数据全部来自一个28nm的1160.04 μm × 1155.6 μm芯片设计，每个窗口是0.5um×0.5um大小，每个窗口高度是三个布线层，每层最多五根平行线。
还用了数据增强，总电容数据集约40000个窗口，耦合电容数据集约45000个窗口。

每个窗口采样1024个点，论文没具体说是怎么采样的，大概是在导体表面采集的。
然后每个点有八个特征：分别是三个左边、这个点所在边界面的三个单位法向量，以及环境介电常数和区分导体用的标志。

实在懒得细看了。

## 开源数据集

### CapBench

参考文献：CapBench: A Multi-PDK Dataset for Machine-Learning-Based  Post-Layout Capacitance Extraction （清华 DAC 2026 CCFA）

本文开源了三种自己处理的数据集标签，可以用于CNN\GCN\点云。用的开源的RTL，用OpenROAD跑到布线，再用KLayout切出很多窗口，再用RWCap标注电容和用Raphael验证RWCap标签精度。

文章使用了三种开源数据集，ASAP7/Nangate45/Sky130，这三个工艺做了五个芯片设计Ibex/JPEG/CAV6/TinyRocket/RocketChip-Benchmark。

论文一共切出了61855个窗口，每个PDK都切有三种窗口大小：
| PDK       |   Small |  Medium |   Large |
| --------- | ------: | ------: | ------: |
| ASAP7     | 0.75 μm |  2.5 μm |  5.0 μm |
| NanGate45 |  2.0 μm |  5.0 μm | 10.0 μm |
| Sky130HD  |  4.5 μm | 10.0 μm | 20.0 μm |

每个工艺、每个窗口大小切出来的数量如下：
| PDK           | 窗口大小   |    边长 L | 金属层范围         | window 数量 |
| ------------- | ------ | ------: | ------------- | --------: |
| **ASAP7**     | Small  | 0.75 μm | M1–M7         |     2,947 |
| **ASAP7**     | Medium |  2.5 μm | M1–M9         |     1,846 |
| **ASAP7**     | Large  |  5.0 μm | M1–M9         |     1,559 |
| **NanGate45** | Small  |  2.0 μm | metal1–metal4 |     8,109 |
| **NanGate45** | Medium |  5.0 μm | metal1–metal7 |     8,106 |
| **NanGate45** | Large  | 10.0 μm | metal1–metal7 |     2,028 |
| **Sky130HD**  | Small  |  4.5 μm | li1–met4      |    17,668 |
| **Sky130HD**  | Medium | 10.0 μm | li1–met5      |    18,220 |
| **Sky130HD**  | Large  | 20.0 μm | li1–met5      |     1,372 |


论文标签用的是自己的RWCap，而不是Raphael场求解器。因为RWCap对比Raphael的总电容和耦合电容误差分别是0.64%和2%，已经很小了。
而且比Raphael快。OpenRCX相比于Raphael的误差在20%很大，所以不适合打标签。